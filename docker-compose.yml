services:
  aplexa:
    image: ghcr.io/mayberts/aplexa:latest
    container_name: aplexa
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PORT=3000
    ports:
      - "3000:3000"
    volumes:
      - aplexa_data:/app/data

volumes:
  aplexa_data:
